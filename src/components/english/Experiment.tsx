// Registers the English dictionary before the experiment modules evaluate; English lesson pages
// hydrate this island so Chinese pages never download the translations.
import '../../i18n/english';
import Experiment from '../Experiment';
export default Experiment;
